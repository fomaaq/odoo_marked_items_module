<odoo>
  <data>
    <record model="ir.ui.view" id="marked_item.list">
      <field name="name">act_of_changes list</field>
      <field name="model">act_of_changes</field>
      <field name="arch" type="xml">
        <form>
          <header>
              <button name="action_apply" type="object" string="Применить акт"/>
          </header>
          <sheet>
            <group>
              <field name="name"/>
              <field name="date"/>
              <field name="applicable_status"/>
              <field name="is_status_buy" invisible="1"/>
              <field name="is_status_sell" invisible="1"/>
              <field name="stock_from" invisible="is_status_buy == True"/>
              <field name="stock_where" invisible="is_status_sell == True"/>
              <field name="product" invisible="is_status_buy == False"/>
              <field name="marked_product" invisible="is_status_buy == True"/>
              <field name="product_quantity" invisible="is_status_buy == False"/>
            </group>
          </sheet>
        </form>
      </field>
    </record>
    <!-- explicit list view definition -->
<!--
    <record model="ir.ui.view" id="marked_item.list">
      <field name="name">marked_item list</field>
      <field name="model">marked_item.marked_item</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
-->

    <!-- actions opening views on models -->
<!--
    <record model="ir.actions.act_window" id="marked_item.action_window">
      <field name="name">marked_item window</field>
      <field name="res_model">marked_item.marked_item</field>
      <field name="view_mode">tree,form</field>
    </record>
-->

    <!-- server action to the one above -->
<!--
    <record model="ir.actions.server" id="marked_item.action_server">
      <field name="name">marked_item server</field>
      <field name="model_id" ref="model_marked_item_marked_item"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": model._name,
        }
      </field>
    </record>
-->

    <!-- Top menu item -->
<!--
    <menuitem name="marked_item" id="marked_item.menu_root"/>
-->
    <!-- menu categories -->
<!--
    <menuitem name="Menu 1" id="marked_item.menu_1" parent="marked_item.menu_root"/>
    <menuitem name="Menu 2" id="marked_item.menu_2" parent="marked_item.menu_root"/>
-->
    <!-- actions -->
<!--
    <menuitem name="List" id="marked_item.menu_1_list" parent="marked_item.menu_1"
              action="marked_item.action_window"/>
    <menuitem name="Server to list" id="marked_item" parent="marked_item.menu_2"
              action="marked_item.action_server"/>
-->
  </data>
</odoo>
